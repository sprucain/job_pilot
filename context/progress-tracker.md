# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 2 — Profile Page
**Last completed:** 07 AI Profile Extraction from Resume
**Next:** 08 Resume PDF Generation from Profile

---

## Progress

### Phase 1 — Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 — Profile Page

- [x] 05 Profile Page — Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [ ] 08 Resume PDF Generation from Profile

### Phase 3 — Find Jobs Page

- [ ] 09 Find Jobs Page — Full UI
- [ ] 10 Adzuna Job Discovery
- [ ] 11 Filter + Sort + Pagination

### Phase 4 — Job Details Page

- [ ] 12 Job Details Page — Full UI
- [ ] 13 Company Research Agent

### Phase 5 — Dashboard

- [ ] 14 Dashboard Page — Full UI
- [ ] 15 Stats Bar — Real Data
- [ ] 16 Recent Activity — Real Data
- [ ] 17 Analytics Charts — PostHog Data

---

## Decisions Made During Build

- **01 Homepage** — Built `components/homepage/Testimonial.tsx` and `components/homepage/CTASection.tsx` (not pre-listed in architecture.md's folder sketch) since the design has a distinct testimonial block and bottom CTA that don't map onto `Hero`/`HowItWorks`/`Features`. Skipped `HowItWorks.tsx` entirely — nothing in landing-page.png corresponds to a separate numbered "how it works" section; the two alternating blocks in `Features.tsx` cover that content.
- **01 Homepage** — Get Started / Find Your First Match / Start for free all link straight to `/login`. Auth doesn't exist yet (Feature 02), so there's no session to branch on; project-overview.md's "logged out → /login" behavior is the only reachable state right now. Revisit once Feature 02 lands to add the authenticated → `/dashboard` branch.
- **01 Homepage** — The near-black CTA buttons in the design map to `--color-overlay-dark` + `--color-accent-foreground`, not the purple `--color-accent` — see ui-registry.md "Button — Primary" entry.
- **02 Auth** — `architecture.md`'s `@insforge/ssr` package does not exist on npm. The real SSR helpers ship as subpath exports of `@insforge/sdk`: `@insforge/sdk/ssr` (`createBrowserClient`, `createServerClient`, `createAuthActions`, `refreshAuth`) and `@insforge/sdk/ssr/middleware` (`updateSession`, purpose-built for Proxy/middleware). Confirmed by downloading the actual package and reading its `SDK-REFERENCE.md` — `architecture.md`/`library-docs.md` should be updated to reference `@insforge/sdk/ssr`, not `@insforge/ssr`.
- **02 Auth** — This project runs Next.js 16.3.4, which renamed `middleware.ts` → `proxy.ts` (exported function `proxy`, not `middleware`) — confirmed in `node_modules/next/dist/docs`. Built `proxy.ts` accordingly. `architecture.md` and `build-plan.md` still say `middleware.ts` — update them to `proxy.ts` in a future pass.
- **02 Auth** — SSR-mode InsForge does not auto-exchange OAuth callbacks in the browser (that only applies to the plain, non-SSR client). Real flow: Server Action (`actions/auth.ts`) calls `createAuthActions().signInWithOAuth(provider, { skipBrowserRedirect: true })`, stores the returned `codeVerifier` in an httpOnly cookie, redirects to the provider. The callback is a **Route Handler** (`app/(auth)/callback/route.ts`), not a page — it calls `exchangeOAuthCode(code, codeVerifier)` and redirects to `/dashboard`. `architecture.md`'s folder sketch lists `callback/page.tsx` — should be `callback/route.ts`.
- **02 Auth (follow-up)** — Homepage CTA branching and the authenticated Navbar variant, deferred during Feature 02 itself, are now done. `app/page.tsx` calls `createInsforgeServer().auth.getCurrentUser()` once and computes `isAuthenticated`/`ctaHref`, passed down to `Navbar` and threaded through `Hero`/`CTASection` into `CTAButtons` (now takes a required `href` prop instead of hardcoding `/login`). `Navbar` takes `isAuthenticated: boolean` + optional `activeRoute?: string` — logged-in variant drops the "Start for free" CTA and highlights the nav link matching `activeRoute` with `text-accent`; no avatar/sign-out control was added, no spec calls for one. See `ui-registry.md` for the updated Navbar/CTAButtons entries.
- **02 Auth** — `/dashboard`, `/profile`, `/find-jobs`, `/find-jobs/[id]` don't exist yet (Phase 2/3/5 features). `proxy.ts` protects those path prefixes regardless, so they'll be covered automatically once built — no stub pages were created to avoid conflicting with their real UI specs later.
- **03 PostHog** — Most of this feature had already been implemented on disk (outside this session — likely a concurrent run of PostHog's own setup wizard, which uses a nearly identical `integration-nextjs-app-router` skill) by the time this session picked it up: `posthog-js`/`posthog-node` installed, `instrumentation-client.ts`, `lib/posthog-server.ts`, `next.config.ts` `/ingest` reverse-proxy rewrites, real `.env.local` values, and PostHog capture calls already added to `actions/auth.ts`/`callback/route.ts` (`login_initiated`, `login_failed`, `login_completed`). Treated as deliberate per the standing instruction to take on-disk changes as current state rather than reverting — verified rather than redone.
- **03 PostHog** — `instrumentation-client.ts` (project root, auto-loaded by Next.js 15.3+/16) is the correct pattern here, not `lib/posthog-client.ts` initialized manually in `app/layout.tsx` as `build-plan.md`/`library-docs.md` originally specified — same category of staleness as the `middleware.ts`→`proxy.ts` and `@insforge/ssr`→`@insforge/sdk/ssr` corrections in Feature 02. Both docs updated. Explicitly never combine `instrumentation-client.ts` with a `PostHogProvider` — would double-initialize.
- **03 PostHog** — Env var is `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN`, not `NEXT_PUBLIC_POSTHOG_KEY` as originally documented — `code-standards.md`'s env var table corrected to match.
- **03 PostHog** — Added `enableExceptionAutocapture: true` to the `lib/posthog-server.ts` PostHog constructor (both the real client and the missing-token no-op fallback) — was missing, and is an explicit framework rule from the PostHog integration skill.
- **03 PostHog** — Formalized `login_initiated`, `login_failed`, `login_completed` into `code-standards.md`'s PostHog Events table (now 7 approved events, was 4) — these were already implemented on disk but violated the table's own "do not add without updating this list first" rule until now. All three are server-side-only events (fired from Server Actions/Route Handlers, not client components) since the OAuth redirect handshake isn't observable from the browser.
- **03 PostHog** — `posthog.reset()` on logout (required by `build-plan.md`'s original Feature 03 spec) is still not wired up. It's a `posthog-js` (browser-only) call and cannot be invoked from the `signOut` Server Action — it needs a client-side trigger alongside whatever UI calls `signOut`. Not built because no sign-out UI exists yet (same gap noted in Feature 02's follow-up). Revisit once a sign-out control is built.
- **03 PostHog** — This session's PostHog integration skill (`integration-nextjs-app-router`) is designed to run inside PostHog's own setup wizard tooling — it expects a `TaskCreate`/`TaskUpdate` tool, a `publish_handoff` tool, and PostHog MCP tools (`mcp__posthog__exec`) for creating a dashboard/notebook and publishing a handoff report. None of those are available in this environment (confirmed via `ToolSearch` — no matches). Extracted and applied the actual engineering guidance (file conventions, event/identify/reset rules, error handling) manually instead; no PostHog dashboard, notebook, or `.posthog-events.json` was created as a result — if a PostHog MCP server gets connected later, revisit `4-conclude.md` in the skill for the dashboard-creation step.
- **04 Database Schema** — Confirmed via `run-raw-sql` before writing any DDL that InsForge's Postgres is Supabase-shaped: `auth.users(id uuid, email, ...)` plus `auth.uid()`/`auth.email()`/`auth.role()`/`auth.jwt()` helper functions, and `anon`/`authenticated` roles already have default-privilege grants (`arwd`) on any new `public` table — so table-level GRANTs are automatic and RLS policies are the only thing actually restricting per-row access. Used `auth.uid()` in every policy accordingly, matching the InsForge SDK's own DB-query rule ("always scope to `user_id`") at the database layer too, not just in app code.
- **04 Database Schema** — All four tables (`profiles`, `agent_runs`, `jobs`, `agent_logs`) created exactly per architecture.md's column list, with RLS enabled and four policies each (select/insert/update/delete, all `USING`/`WITH CHECK (user_id = auth.uid())`, `profiles` using `id = auth.uid())` since its PK *is* the user id). Added a few things architecture.md doesn't spell out but that were clearly implied: `profiles.id` has an explicit `REFERENCES auth.users(id) ON DELETE CASCADE`; foreign keys `agent_runs.user_id`/`jobs.user_id`/`agent_logs.user_id` all `REFERENCES profiles(id) ON DELETE CASCADE`; `jobs.run_id` and `agent_logs.job_id` are nullable with `ON DELETE SET NULL` (matches "null if from URL input" in architecture.md); `jobs.source` and `agent_logs.level` got `CHECK` constraints enforcing their documented enum values; indexes added on every foreign key plus `jobs.match_score`/`jobs.found_at` since Feature 11 (Filter + Sort + Pagination) will sort/filter on exactly those; a `profiles_set_updated_at` trigger auto-maintains `updated_at` on every update (`created_at`/`updated_at` both `DEFAULT now()`).
- **04 Database Schema** — `resumes` storage bucket created with `isPublic: false`. Architecture.md's "own files only" half of the access rule is **not** enforced by Postgres RLS — `storage.objects` has no `owner`/`user_id` column comparable to `auth.uid()` (only `uploaded_by text`, `bucket`, `key`), so per-user path isolation for `resumes/{user_id}/resume.pdf` relies on the InsForge SDK/API layer checking the authenticated caller against the path, not on a custom RLS policy written here. Flagging so Feature 06 (Profile Save Logic, which does the actual upload) doesn't assume a database-level guarantee that isn't there.
- **04 Database Schema — `/review` findings resolved (2026-09-08)** — `/review` found one Important and three Minor issues. Fixed: (1) **Important** — `agent_runs.status` had no `CHECK` constraint even though `jobs.source`/`agent_logs.level` (the same category of documented enum column in architecture.md) both did; added `agent_runs_status_check CHECK (status IN ('running','completed','failed'))`, confirmed via `pg_constraint`. (2) **Minor** — dropped the `DELETE` policy on all four tables (`profiles_delete_own`, `agent_runs_delete_own`, `jobs_delete_own`, `agent_logs_delete_own`); no feature in build-plan.md performs a delete anywhere (job-dismiss and profile-deletion are both explicitly out of scope per project-overview.md), so RLS now denies DELETE entirely by default rather than granting unrequested scope — confirmed via `pg_policies`, 12 policies remain (select/insert/update × 4 tables). A future feature that genuinely needs deletion should add its own policy deliberately rather than relying on this leftover one. (3) **Minor** — struck build-plan.md's Feature 04 "tailored fields" bullet for the `jobs` table; it contradicted both architecture.md's actual schema and project-overview.md's explicit "Resume tailoring per job" / "Score recalculation after tailoring" out-of-scope entries — nothing was ever built for it, this just removes the stale instruction. (4) **Minor** — corrected build-plan.md's Feature 16 text, which said to sort `agent_runs` by "created_at descending" — that column doesn't exist on `agent_runs` (only `started_at`/`completed_at`, per architecture.md); now reads "started_at (agent_runs) / found_at (jobs) descending" to match the real schema.
- **04 Database Schema — unresolved verification gap (documented, not fixed)** — `/review` also flagged that RLS's actual cross-user isolation was verified only structurally (policies exist, reference `auth.uid()` correctly), not empirically — `run-raw-sql` always executes as `postgres` (`rolbypassrls = true`) and the MCP tool explicitly rejects `SET ROLE`/`set_config()` ("Changing SQL execution role... is not allowed"), so this sandbox has no way to simulate a second authenticated user's session. This is an environment limitation, not something fixable in this session — carry it forward: confirm real cross-user isolation once two real user sessions exist (or via an authenticated PostgREST/SDK call with two different real JWTs), rather than assuming the structural check is sufficient.
- **05 Profile Page — Full UI** — Ran `/architect` before building (first complex multi-section feature since Feature 02) to align on scope: mock data only (one hardcoded `Profile` object, no persistence), a real reusable `lib/profile-completion.ts` (not hardcoded 70%) so Feature 06 can reuse the same logic for `is_complete`, per-section component split beyond architecture.md's single `ProfileForm.tsx` sketch, and deliberately skipping `ResumePreview.tsx` since no design/mock state needs it yet. All four were confirmed with the developer before writing code.
- **05 Profile Page — Full UI** — Reverse-engineered the exact "required fields" rule that produces the design's 70% / PHONE, LOCATION, EDUCATION-missing state: `full_name`, `email`, `phone`, `location`, `current_title`, `experience_level`, `years_experience`, `skills`, `work_experience`, `education` (10 fields — `education` counts complete only when degree/field/institution/year are all filled). `linkedin_url`, `portfolio_url`, `work_authorization`, `job_titles_seeking`, `remote_preference`, `salary_expectation`, `preferred_locations` are NOT counted (some are explicitly marked OPTIONAL in the design, the rest are treated as enrichment rather than core-completion fields). This rule lives in `lib/profile-completion.ts`'s `computeProfileCompletion()` and is not spelled out anywhere in architecture.md/build-plan.md — it was inferred purely from matching the mockup's numbers, so revisit if a future feature's spec ever states required fields explicitly and they don't match.
- **05 Profile Page — Full UI** — `build-plan.md`'s Feature 05 spec text lists a "Cover Letter Tone dropdown" under Job Preferences, but `context/designs/profile.png` has no such field anywhere — Job Preferences ends at Preferred Locations, then directly to Save Profile (confirmed via close crop of the mockup's bottom section). Per AGENTS.md's "design assets are source of truth for visual decisions," built exactly what the mockup shows and omitted the field. `profiles.cover_letter_tone` still exists as a DB column (Feature 04) and the type (`CoverLetterTone` in `types/index.ts`) is defined for future use, but no UI writes to it yet — worth a build-plan.md correction in a future pass, same category as the Feature 04 stale-doc fixes.
- **05 Profile Page — Full UI** — `ProfileForm.tsx` is the single `"use client"` component owning all state; the completion ring and missing-field tags recompute live as the user types (verified via Playwright: filling Phone moved 70% → 80% and dropped the PHONE tag in real time), not static values — a deliberate refinement beyond the literal build-plan text since "fully interactive" was the agreed scope. Work Experience rows are capped at 3 (`MAX_WORK_EXPERIENCE_ENTRIES`), any role can be removed once 2+ exist (not just non-first ones), and "Currently working here" clears + disables End Date.
- **05 Profile Page — Full UI** — First feature needing `types/index.ts` (didn't exist before) — added `Profile`, `WorkExperienceEntry`, `Education`, and the enum-shaped string unions (`ExperienceLevel`, `WorkAuthorization`, `RemotePreference`, `CoverLetterTone`) matching architecture.md's `profiles` table columns exactly (camelCase in the TS type, snake_case in the DB — Feature 06's Server Action will need to map between them on save/load).
- **05 Profile Page — Full UI — `/review` findings resolved (2026-09-09)** — `/review` found 1 Important and 2 Minor issues, all fixed: (1) **Important** — the three `<select>` handlers narrowing `event.target.value` to a union type (`ExperienceLevel`/`WorkAuthorization`/`RemotePreference`) used `as` type assertions with no explanatory comment, violating code-standards.md's explicit rule; added a one-line comment to each explaining why the assertion is safe (every `<option>` value is drawn from the same union). (2) **Minor** — `inputClass`/`labelClass` were copy-pasted verbatim across 6 files; extracted to `lib/form-styles.ts` and every profile component now imports from there. (3) **Minor** — form `<label>` elements had no `htmlFor`/`id` pairing with their inputs (clicking a label didn't focus the field); added explicit ids to every field (`${entry.id}-<field>` for repeatable Work Experience rows, `useId()` in `TagInput.tsx` since it's reused twice on one page). Re-verified after the fix: `tsc`/`eslint` clean, Playwright confirmed clicking a label now focuses its input and the live completion recalculation still works (70%→80% after filling Phone), and `proxy.ts` still correctly redirects unauthenticated `/profile` visits to `/login`.
- **06 Profile Save Logic** — Ran `/architect` before building. Three scope decisions locked in with the developer: (1) one combined "Save Profile" Server Action handles both the resume upload and all other fields in a single click, not two separate flows; (2) `jobTitlesSeeking`/`preferredLocations` stay plain free-text inputs in the UI (per ui-registry.md's design decision) but convert to the DB's `text[]` columns via comma-split-and-trim on save / comma-join on load; (3) `ResumePreview.tsx` stays deferred — `ResumeUpload.tsx` only gets a minimal "Resume on file" indicator.
- **06 Profile Save Logic** — Real InsForge SDK type definitions (`node_modules/@insforge/sdk/dist/*.d.ts`), not just `library-docs.md`'s prose, were the deciding source for the exact API surface, and corrected several stale assumptions: DB/storage access is namespaced (`insforge.database.from(...)`, `insforge.storage.from(...)`, not top-level `.from()`); `.maybeSingle()` exists (standard postgrest-js) for clean zero-row reads; `StorageBucket.upload(path, file)` takes no options object at all — no `upsert`/`contentType` flags — because uploading to an existing key always replaces it in place, so the "with `upsert: true`" phrasing in build-plan.md/library-docs.md turned out to already be the default behavior, not a flag to pass. `library-docs.md` should get a pass to correct its storage-upload example, same category as the `@insforge/ssr`→`@insforge/sdk/ssr` and `middleware.ts`→`proxy.ts` corrections from Feature 02.
- **06 Profile Save Logic** — The `resumes` bucket is private (`public: false`), and the SDK only exposes `getPublicUrl()` (public buckets only, no network call) or `createSignedUrl()` (private buckets, expires — max 7 days) for a fetchable link — neither is a permanent URL. Since `profiles.resume_pdf_url` is a persisted column, it now stores the storage **key** (`${userId}/resume.pdf`), not a URL — any future feature needing an actual clickable download link (e.g. a real `ResumePreview.tsx`) should mint a fresh `createSignedUrl()` from that key on demand rather than trusting a stored link. Flagging since the column name says "url" but the persisted value is a key — a deliberate, documented deviation, not an oversight.
- **06 Profile Save Logic** — Added `lib/profile-mapping.ts` as the single camelCase(UI)↔snake_case(DB) mapping source, used by both the read path (`app/profile/page.tsx`) and the write path (`actions/profile.ts`) so they can't drift apart. `cover_letter_tone` is never touched by either direction (no UI field exists for it, consistent with Feature 05's decision to omit the dropdown). `work_experience` entries have their client-only `id` stripped before persisting to jsonb and get a fresh `crypto.randomUUID()` regenerated on load.
- **06 Profile Save Logic** — `zod` (already listed as an approved-but-unused dependency in code-standards.md) is now actually used — `actions/profile.ts` validates the full `ProfileInput` shape server-side before writing, since a Server Action is a public endpoint regardless of what the `<select>` dropdowns constrain in the browser. File type/size (PDF, ≤5MB) stays a plain manual check, mirrored client-side in `ResumeUpload.tsx` for fast feedback.
- **06 Profile Save Logic** — `profile_completed` (one of the 7 approved PostHog events) fires from `actions/profile.ts` only on the `is_complete` `false→true` transition — a pre-read of the existing row's `is_complete` is needed either way to decide insert-vs-update (the table starts empty, no signup trigger creates a row), so detecting the transition came for free. Uses the same `getPostHogClient()`/`flushPostHogSafely()` pattern as `actions/auth.ts`.
- **05 Profile Page — Full UI — second `/review` pass findings resolved (2026-09-09)** — a follow-up `/review` (re-reading the fixed files fresh) found 2 Important and 1 Minor issue: (1) **Important** — `CompletionIndicator.tsx` had no "complete" state; verified live that filling Phone/Location/Education (the exact 3 fields the mockup starts missing) drives the ring to 100% while the banner still read "Profile needs attention" in red with the "Complete the missing fields..." copy — a real bug reachable through completely ordinary interaction given completion was deliberately built to be live. Fixed: added an `isComplete = missingFields.length === 0` branch — green ring/`CheckCircle2` icon/"Profile complete" heading/positive copy when true, unchanged red "needs attention" state otherwise. Re-verified via Playwright screenshot: 100% now renders the success state correctly, 70% still renders identically to before. (2) **Minor** — `WorkExperienceSection.tsx` had no empty state for zero entries (ui-rules.md requires one for every section that can be empty); unreachable through this session's mock data or even through the UI's own remove flow (the remove button never shows when only 1 entry remains, so a user can never remove their way down to 0) — but a real Feature 06 profile load for a brand-new user will start at 0. Fixed: added a dashed-border empty state (muted icon + "No work experience added yet." + Add role button) matching the Resume dropzone's visual language; verified by temporarily setting the mock's `workExperience` to `[]` and confirming both the empty state renders and its Add role button works, then reverted the mock data change. (3) **Important, resolved as a documented exception rather than a code change** — the review flagged that `ResumeUpload.tsx` (card → dropzone → button) and `WorkExperienceSection.tsx` (card → nested entry card → inputs) both stack 3 levels of border-radius, against ui-rules.md's literal "never stack more than 2" invariant. Both exactly match `profile.png`, and AGENTS.md states design assets are the source of truth for visual decisions — so rather than degrading the visual design to satisfy the rule, amended ui-rules.md's invariant with an explicit "unless an approved design mockup shows 3+" carve-out naming these two cases, so future sessions don't re-flag the same confirmed exception. Don't extend this exception to any future 3-level nesting that isn't directly traced to a mockup.
- **Sign Out UI (2026-09-14)** — Closed the long-standing gap flagged since Feature 02/03: no sign-out control existed anywhere, which also blocked wiring up `posthog.reset()` on logout. Added `components/layout/SignOutButton.tsx` (client component, Ghost-button styling — see ui-registry.md) rendered by `Navbar` in the CTA slot whenever `isAuthenticated`. It wraps the existing `signOut` Server Action (`actions/auth.ts`, already built but never called from any UI) in a `<form action={...}>` that calls `posthog.reset()` first, since that call is browser-only and has no server-side equivalent — it must run before the action's `redirect("/login")`. No mockup (`navbar_crop.png`, `dashboard.png`) shows a sign-out control, so the minimal Ghost-button variant was used rather than inventing a heavier avatar/dropdown pattern not specified anywhere. No new PostHog event was needed — `reset()` isn't a capture call, code-standards.md's Events table is unchanged. Verified via `tsc --noEmit`/`eslint .` (clean); both pages that render an authenticated `Navbar` (`app/page.tsx`, `app/profile/page.tsx`) pick it up automatically.
- **Resume preview + delete (2026-09-14)** — Developer confirmed via a real logged-in session that Feature 06's save flow works end to end (closing the verification gap noted above), then asked for two follow-on improvements to `ResumeUpload.tsx`: an inline PDF preview once a resume is uploaded, and the ability to delete a previously uploaded one. Both built: (1) **Preview** — `app/profile/page.tsx` now mints a signed URL (`insforge.storage.from("resumes").createSignedUrl()`) for the persisted resume on every page load, and `actions/profile.ts`'s `saveProfile` mints a fresh one after every successful save too (`SaveProfileResult.data` gained `resumePreviewUrl` alongside the existing `resumePdfUrl` key) — both non-fatal if signing fails (falls back to "Preview unavailable" rather than breaking the save/load). A just-picked, not-yet-saved file previews instantly client-side via `URL.createObjectURL()` instead of waiting on a round trip. (2) **Delete** — new `deleteResume()` Server Action: looks up the current `resume_pdf_url` key, calls `insforge.storage.from("resumes").remove(key)`, then clears the column, following the exact same auth/scoping pattern as `saveProfile`. Gated by a native `window.confirm()` — no confirm-dialog component exists yet, so this deliberately doesn't introduce one for a single delete button. `ResumeUpload.tsx`'s dropzone is now only shown when there's no resume at all; see ui-registry.md's "Resume preview + delete" entry for the full visual spec. Verified via `tsc --noEmit`/`eslint .` (clean) and an isolated, unauthenticated scratch route (`app/dev-preview-test-scratch/page.tsx`, deleted after) that mounted `ResumeUpload` directly with mock props for all three states (empty / persisted / pending) and screenshotted each via Playwright — zero console errors, delete button correctly absent in the pending-only state. Did **not** re-verify the real signed-URL round trip against a live authenticated session (same sandbox limitation as before — no bypass of `proxy.ts` was attempted this time after the prior session's `[Security Weaken]` classifier block); the developer should confirm the inline preview actually renders their real uploaded PDF (not just that the code compiles) on their next real session.
- **Sign Out UI + Resume preview/delete — `/review` findings resolved (2026-09-14)** — `/review` found 2 Important issues, both fixed: (1) **Important** — `SignOutButton.tsx`'s `posthog.reset()` call had no try/catch; if it threw, the form action would reject before `await signOut()` ever ran, meaning an analytics failure could leave a user unable to sign out at all — the exact failure class Feature 03 already fixed elsewhere in this codebase (`flushPostHogSafely()`) but hadn't been applied here. Wrapped in try/catch so `signOut()` always runs regardless. (2) **Important** — `ResumeUpload.tsx`'s "Remove" button rendered (and would delete the persisted resume) even while a `pendingFile` replacement was selected; clicking it also silently cleared the pending selection via `ProfileForm`'s `handleDeleteResume` (`setResumeFile(null)`), with no indication why — reachable through completely ordinary interaction (pick a replacement, then click Remove expecting to just clear the old one). Fixed by hiding "Remove" whenever a `pendingFile` is set (`existingResumeUrl && !pendingFile`) — Replace/Save supersede it during that window. Re-verified via the same scratch-route Playwright technique: Remove correctly visible with only a persisted resume, correctly hidden while a replacement is pending, zero console errors. `tsc --noEmit`/`eslint .` clean. One Minor issue (re-signing a preview URL on every save even when the resume didn't change) was noted but left as-is — an avoidable extra network call, not a bug.
- **AI provider swap (2026-09-14/15, precedes Feature 07)** — Swapped the project's AI provider project-wide from OpenAI GPT-4o to Venice AI (GLM 5.2, OpenAI-compatible), not scoped to just one feature. `lib/venice-client.ts` (new) exports `getVeniceClient()` + `AI_MODEL = "zai-org-glm-5-2"`, mirroring `lib/posthog-server.ts`'s singleton pattern — every AI call site must import from there, never construct `new OpenAI(...)` directly. **GLM 5.2 is a reasoning model** — every call must pass `venice_parameters: { disable_thinking: true }` or `content` can come back empty at low `max_tokens` (confirmed by live testing: reasoning tokens consumed the entire budget). Max-token budgets: matching/scoring 300, company research synthesis 800, resume generation 1000, profile extraction 800. Stagehand's own model config (Feature 13, not yet built) was deliberately **not** swapped — unverified whether Stagehand supports Venice's base-URL pattern, flag for confirmation before building Feature 13. `library-docs.md`, `code-standards.md`, `architecture.md`, `project-overview.md`, `build-plan.md` all updated to reference Venice/GLM 5.2 instead of GPT-4o.
- **07 AI Profile Extraction from Resume** — Ran `/architect` (continued across a session boundary via `/remember`) to lock four decisions before building: (1) extraction logic lives in `lib/resume-extraction.ts`, not `agent/extractor.ts` — this is a synchronous, single-shot, user-clicked action with no `run_id`/`job_id`, unlike the background-style operations `agent/` is designed for, and `agent/extractor.ts` doesn't exist yet/doesn't match this use case; no `agent_logs` entry for the same reason. (2) Extraction reads whatever PDF is currently loaded client-side (just-picked `pendingFile`, or the persisted resume re-fetched from its signed preview URL) — no DB write required first. (3) Only resume-derivable fields are touched (`fullName`, `phone`, `location`, `linkedinUrl`, `portfolioUrl`, `currentTitle`, `experienceLevel`, `yearsExperience`, `skills`, `industries`, `workExperience` capped at 3, `education`) — search-preference fields (`jobTitlesSeeking`, `remotePreference`, `preferredLocations`, `salaryExpectation`, `workAuthorization`) are never touched, a resume can't inform them. (4) Overwrite behavior: fields currently empty fill silently; fields that already hold a value only change through a new inline review panel (`ExtractionReviewPanel.tsx`) showing old-vs-new per field with a checkbox, defaulting checked — an explicit upgrade beyond `build-plan.md`'s original "populate automatically, user edits before saving" text, confirmed with the developer. `workExperience`/`education`/`skills`/`industries` are reviewed as one whole-field row each (summarized, e.g. "3 roles: Acme Corp (Eng), ...") — no per-array-entry or per-subfield control. `experienceLevel` always goes through the review panel rather than silent-fill, since `mapProfileRowToUi` defaults it to `"junior"` — there's no way to tell "never set" apart from "genuinely junior" (same ambiguity `lib/profile-completion.ts`'s `isFilled` check already has to live with).
- **07 AI Profile Extraction from Resume — `/review` findings resolved (2026-09-15)** — `/review` found 3 Important and 2 Minor issues, all fixed: (1) **Important** — `handleExtract` (`ProfileForm.tsx`) diffed the extraction response against a stale, click-time snapshot of `profile`; if the user edited a field (e.g. typed a phone number) while the multi-second Venice call was in flight, and that field was empty at click time, the extraction's own value would silently overwrite the user's fresh edit on completion — bypassing the review panel entirely, since the panel only covers fields non-empty *at click time*. Fixed with a `profileRef` kept in sync via `useEffect` (not written during render — `react-hooks/refs` disallows that) so `handleExtract` always diffs against the latest profile state, not the one from click time. (2) **Important** — `ExtractionReviewPanel.tsx` nested 3 levels of border-radius (Profile Information card → panel → per-row box) with no approved mockup, against `ui-rules.md`'s explicit 2-level cap; the only two whitelisted 3-level exceptions are both mockup-traced and this isn't one of them. Fixed by dropping the per-row `rounded-md` box in favor of `divide-y`-separated rows (same pattern `ProfileForm.tsx`'s own section list already uses), leaving only 2 radius levels (card, panel). (3) **Important** — six uncommented type assertions across `lib/resume-extraction.ts`, `lib/extraction-review.ts`, and `ProfileForm.tsx`, against `code-standards.md`'s explicit "never use type assertions unless commented why" — a rule this same feature area otherwise follows carefully (the `<select>` handlers each carry one). All six now have a one-line comment explaining why the assertion is safe. (4) **Minor** — the `experienceLevel` conflict row showed the raw enum value ("junior"/"senior") instead of the human labels ("Junior"/"Senior") `ProfessionalInfoSection.tsx` already defines for the same field; extracted those labels into a new shared `lib/profile-options.ts` (`EXPERIENCE_LEVEL_OPTIONS` + `experienceLevelLabel()`) that both files now import from, rather than each defining its own copy. (5) **Minor** — `MAX_RESUME_SIZE_BYTES` (5MB) was independently defined in three files (`ResumeUpload.tsx`, `actions/profile.ts`, the new route handler) and `MAX_WORK_EXPERIENCE_ENTRIES` (3) in three as well (`ProfileForm.tsx`, `lib/resume-extraction.ts`, and a hardcoded `3` in `actions/profile.ts`'s zod schema); centralized both into a new `lib/constants.ts`, all five call sites now import from there. Re-verified after all fixes: `tsc --noEmit`/`eslint .` clean, and a fresh live extraction request through the real pdf-parse → Venice → structured-JSON pipeline still returns 200 with correct data.
- **07 AI Profile Extraction from Resume — `pdf-parse` version mismatch discovered** — `code-standards.md`/`library-docs.md` listed `pdf-parse` as approved, but it had never actually been installed — this was the first feature to need it. The installed version (2.4.5) turned out to be a complete v2 rewrite with a different API than what `library-docs.md` documented (v1's `pdf(buffer)` default-export call doesn't exist anymore; v2 is a `PDFParse` class with `new PDFParse({ data: buffer }).getText()`, and requires `await parser.destroy()`). Discovered by live testing, not assumed — `library-docs.md`'s pdf-parse section rewritten to match. Also required adding `serverExternalPackages: ["pdf-parse"]` to `next.config.ts`: without it, Turbopack bundles `pdf-parse`'s `pdfjs-dist` dependency into the Server Components bundle, which breaks the relative-path worker it tries to spin up (`Setting up fake worker failed`) — confirmed by live testing against a real dev server, fixed, then re-verified with a real generated PDF through the full pdf-parse → Venice → structured-JSON pipeline (200 success) and a deliberately malformed PDF (422, correct user-facing error message).

---

## Notes

- Dev server for job_pilot already had an existing instance running (PID from a prior session) bound to :3000 — reused it rather than force-killing; a second `npm run dev` attempt correctly detected the conflict and no-op'd on :3001, which was then stopped.
- Headless browser tooling (Playwright/Chromium) is available in this environment — the browser binary just needs `npx playwright install --with-deps chromium` re-run if the container was reset since the last session (happened during Feature 02).
- **Environment change, discovered during Feature 06** — starting the dev server (`npm run dev`, foreground or backgrounded) is now blocked outright by this sandbox's auto-mode permission classifier, regardless of syntax (`&`, `run_in_background`). The Playwright-based UI verification technique used successfully in Features 02/03/05 is **no longer available** until a human approves that permission. Feature 06 was instead verified via `tsc --noEmit`/`eslint .` (clean) plus a direct InsForge `run-raw-sql` round-trip (insert → update → delete, cleaned up afterward) against the real `profiles` table using the developer's real dev account (`spruce.wain@gmail.com`, confirmed via `auth.users`) to validate every array/jsonb column shape and the `updated_at` trigger match exactly what `actions/profile.ts`/`lib/profile-mapping.ts` produce. The actual Server Action code path (real auth session → `saveProfile()` → real insert/update → `profile_completed` firing) was **not** exercised end-to-end — no live OAuth session obtainable here, same limitation as every prior feature's auth-gated testing, compounded now by the dev-server block. Revisit this permission with the developer before the next feature if live UI verification is needed again.
- **Environment change, discovered 2026-09-14 while attempting to verify Feature 06's real save flow and the new Sign Out UI** — the dev-server block from the Feature 06 session is gone (`npm run dev` runs fine now, an instance was already up on :3000), and Playwright/Chromium reinstalled cleanly. But the bypass-auth-and-screenshot technique used successfully in Feature 05 (temporarily removing a route from `proxy.ts`'s `PROTECTED_PREFIXES` + commenting out a page's `redirect("/login")`) is **no longer available**: the sandbox's auto-mode permission classifier now hard-blocks any subsequent tool call once `proxy.ts` has an auth prefix removed, flagged `[Security Weaken]` — a stricter policy than existed during Feature 05. Reverted the edit immediately (`git diff` confirmed clean) rather than attempting to route around the block. A separate, unrelated classifier block (`[PII Data Handling]`) also fired when a scratch script had a real user's email hardcoded in it — confirms real user PII (even the developer's own test account, `spruce.wain@gmail.com`) should never be pasted into scratch scripts/args in this environment, even locally. **Net effect: Feature 06's actual Server Action code path (real login → fill form → Save → reload → confirm pre-fill) is still not verifiable from this sandbox** — same conclusion as the Feature 06 session, now confirmed unfixable by working around the dev-server block alone. The only path to closing this gap is the developer doing a real click-through in their own browser, same as Feature 02's "real-session confirmation" note below.
- **02 Auth** — Verified via `tsc --noEmit`, `eslint .`, a real Playwright screenshot of `/login` (zero console errors), curl checks confirming `proxy.ts` redirects `/dashboard`, `/profile`, `/find-jobs` → `/login` while `/` stays public, and a full click-through of "Continue with Google" that reached Google's real sign-in screen with the `insforge_oauth_code_verifier` cookie set correctly. Could not verify the final leg (`exchangeOAuthCode` after a real provider login) — no live Google/GitHub credentials in this sandbox. Backend's `allowedRedirectUrls` is currently empty and didn't block the OAuth kickoff in testing, but flagging in case it affects other redirect-based flows (email verification/reset links) later.
- **02 Auth (follow-up)** — Verified via `tsc --noEmit`, `eslint .`, and Playwright: confirmed the real DOM (not raw HTML text, which is misleading here — Next inlines a duplicate RSC payload alongside the visible markup) shows exactly one "Start for free" and `/login`-targeted CTAs when logged out, and confirmed an invalid/bogus `insforge_access_token` cookie degrades gracefully to the logged-out variant rather than crashing (`getCurrentUser()` returns a null user, no throw). The authenticated-render branch itself (real logged-in session) is still unverified for the same reason as Feature 02 — no live OAuth session obtainable in this sandbox.
- **07 AI Profile Extraction, dev-server note (2026-09-15)** — The long-running dev server on :3000 could not be stopped/restarted via `kill <pid>` from this Bash tool — the PIDs `ps aux` reported for it were not the same PIDs that actually owned the port (likely a separate process/PID namespace from whatever manages that terminal, e.g. the IDE). `kill` silently no-op'd ("no such process") while the server kept running. What worked: delete the stale `.next/dev/lock` file and start a fresh `npm run dev &` — Next.js's own single-instance lock was the only thing actually blocking a second instance, and once that lock was cleared a new instance bound to :3000 cleanly. If a future session needs to restart the dev server and `kill` doesn't seem to take effect, try this instead of assuming the process is unkillable.
- **02 Auth (follow-up) — real-session confirmation** — The developer completed a real Google login in their own Firefox browser and confirmed the authenticated Navbar/CTA branch renders correctly: no "Start for free" button, "Get Started" points at `/dashboard`. That route then 404s, which is expected — `/dashboard` has no `page.tsx` yet (Phase 5, feature 14, not built). Not a bug; confirmed via `/recover` on 2026-09-08 and parked until the Dashboard feature is built. Private-window testing also confirmed the logged-out variant still renders correctly, ruling out a stuck/cached session.
- **02 Auth — `/review` findings resolved (2026-09-08)** — `/review` flagged three error-handling gaps against `code-standards.md`'s "every Server Action/Route Handler has a try/catch, never expose raw error internals" rules: (1) `proxy.ts` had no try/catch around `updateSession()` — Critical, since Proxy runs on every request and an uncaught throw would have 500'd the whole site, not just auth pages; now falls back to treating the session as unauthenticated on failure (protected routes redirect to `/login`, public routes render normally). (2) `actions/auth.ts`'s Server Actions had no try/catch — restructured so `redirect()` always runs outside the try block (network/SDK failures are caught and logged, `redirect()` itself is never wrapped, avoiding any `NEXT_REDIRECT`-swallowing risk). (3) Both `actions/auth.ts` and `callback/route.ts` were putting raw `error.message` into the `/login?error=...` redirect URL — replaced with static codes (`oauth_start_failed`, `oauth_exchange_failed`, `missing_code`) so no InsForge-internal error text reaches the browser's address bar. Re-verified after the fix: `tsc`/`eslint` clean, all proxy redirects and the login page still 200/307 as expected, and a full Playwright click-through of "Continue with Google" still reaches Google's real sign-in screen with zero console errors.
- **03 PostHog** — Verified via `tsc --noEmit`, `eslint .`, and Playwright: zero console errors on `/` and `/login`, confirmed `/ingest/*` requests actually reach PostHog through the `next.config.ts` rewrites (config.js, flags, exception-autocapture, surveys all loaded successfully from the EU host), and a full click-through of "Continue with Google" still reaches Google's real sign-in screen with the `login_initiated` server-side capture firing alongside it. Did not verify events actually land in the PostHog project dashboard (no PostHog MCP/dashboard access in this environment) — only that the requests are sent and nothing crashes or logs a client/server error.
- **05 Profile Page — Full UI** — Verified via `tsc --noEmit`, `eslint .`, and Playwright: zero console errors on `/profile`, full visual match against `context/designs/profile.png` (banner, resume card, all five form sections), and interaction tests confirming skill tags add/remove, work-experience roles add/remove (capped at 3, any role removable once 2+ exist), the "Currently working here" checkbox correctly disabling/enabling End Date, and the completion ring/missing-tags recomputing live (70%→80% after filling Phone). Confirmed `proxy.ts` still redirects unauthenticated `/profile` visits to `/login` (307) after testing. To screenshot the real form (no live OAuth session in this sandbox — same limitation as Feature 02), temporarily commented out the page's `redirect("/login")` and removed `/profile` from `proxy.ts`'s `PROTECTED_PREFIXES`, screenshotted, then reverted both edits exactly — `git status` confirms only the real feature files remain. Same technique is available for future protected-page UI verification in this environment.
- **03 PostHog — `/review` findings resolved (2026-09-08)** — `/review` found the on-disk PostHog wiring could break the app it was instrumenting: (1) **Critical** — none of the three `await posthog.flush()` calls were wrapped in try/catch, and `@posthog/core`'s own source confirms `flush()` throws on network/HTTP errors; a PostHog outage could have crashed a login attempt, including a *successful* one never reaching its `/dashboard` redirect. Fixed with a shared `flushPostHogSafely()` helper in `lib/posthog-server.ts` — every flush call now routes through it. (2) **Important** — the login funnel couldn't be correlated per-person: `login_initiated` used a random UUID that was immediately discarded, while `login_failed`/`login_completed`'s anonymous branch used the literal string `"anonymous"` for every anonymous visitor, merging unrelated people into one fake PostHog identity. Fixed by persisting the per-attempt UUID in a new short-lived cookie (`POSTHOG_LOGIN_DISTINCT_ID_COOKIE`, same lifecycle as the OAuth `codeVerifier` cookie) and reusing it through `login_failed`/`login_completed`, plus a `posthog.alias()` call linking the anonymous attempt to the real `user.id` on success so pre-login events aren't orphaned. (3) **Minor** — the "missing `code` query param" callback path fired no event at all; now captures `login_failed` with `reason: "missing_code"`. Re-verified: `tsc`/`eslint` clean, OAuth click-through still reaches Google with the new cookie set correctly, and both callback failure paths (`missing_code`, bad/expired code) still redirect cleanly with events firing. `code-standards.md`'s PostHog section corrected to describe the fixed pattern instead of the flawed one it previously documented.
